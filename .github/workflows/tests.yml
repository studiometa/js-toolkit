name: Tests
on: pull_request
jobs:
  Unit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: 14
    - name: Install modules
      run: npm ci
    - name: Run tests
      run: npm run test
