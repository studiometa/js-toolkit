/**
 * Chunk an array into groups.
 */
export function chunks<T>(arr: T[], length = 1):T[][] {
  const chunked = [];

  // create chunks from arr.

  return chunked;
}


/**
 * Chunk an array into groups with overlapping sequences.
 */
export function slidingWindowChunks<T>(arr: T[], length = 1):Array<T[]> {
  const chunked = [];
  return chunked;
}
